apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: linkscope
spec:
  selector:
    app: linkscope-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
